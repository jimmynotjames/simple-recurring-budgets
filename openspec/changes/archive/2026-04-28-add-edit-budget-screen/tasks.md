## 1. ViewModel — `AddEditBudgetViewModel`

- [x] 1.1 Create `simple-recurring-budgets/Views/AddEditBudgetViewModel.swift` declaring `@Observable @MainActor final class AddEditBudgetViewModel` with stored properties `name: String`, `allocation: Decimal`, `currencyCode: String`, `period: BudgetPeriod`, `isCarryOverEnabled: Bool`, plus a private `mode: Mode` enum (`case add` / `case edit(Budget)`).
- [x] 1.2 Add `init(settings: AppSettings)` that seeds the Add-mode defaults exactly as documented in design.md → Decision 2 (and the spec): `name = "Budget"`, `allocation = 10`, `currencyCode = Locale.current.currency?.identifier ?? "USD"`, `period = .daily`, `isCarryOverEnabled = settings.defaultCarryOverEnabled`. Set `mode = .add`. **Do not** keep a stored reference to `settings`; read it once in the initialiser only.
- [x] 1.3 Add `init(editing budget: Budget)` that seeds every field from the passed-in `Budget`: `name = budget.name`, `allocation = budget.allocation`, `currencyCode = budget.currencyCode`, `period = BudgetPeriod(rawValue: budget.period) ?? .daily`, `isCarryOverEnabled = budget.isCarryOverEnabled`. Set `mode = .edit(budget)`.
- [x] 1.4 Add a pure computed property `var canSave: Bool { ... }` per design.md → Decision 4 ("Edge cases for canSave"): `true` iff `name.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty == false` AND `allocation > 0`.
- [x] 1.5 Add `func save(context: ModelContext)` (NO `settings` parameter — see design.md → Decision 1's "On the `save(...)` signature" sub-section: §2.1's "AppSettings similarly when relevant" rider does not apply because the save body never reads `AppSettings`) that branches on `mode`:
    - `.add` → build `Budget(name:allocation:currencyCode:period:resetCadence:isCarryOverEnabled:)` passing `resetCadence: nil` so the model's `.never` default (paused) is used; set `budget.sortOrder = (try? Budget.nextSortOrder(for: context)) ?? 0`; `context.insert(budget)`; `try? context.save()`.
    - `.edit(let budget)` → field-by-field comparison rewrite per design.md → Decision 8: only mutate fields whose stored value differs; if at least one field changed, set `budget.lastModified = Date()` and `try? context.save()`. Do NOT touch `carryOverAmount`, `carryOverLastProcessedDate`, `carryOverLastResetDate`, `resetCadence`, `sortOrder`, or `createdAt`.
- [x] 1.6 Confirm the VM does NOT have any stored property of type `ModelContext`, does NOT have any stored property of type `AppSettings`, does NOT have any stored property of type `CurrencyDisplayPreference`, and does NOT call `@Query` or any fetch (verified with grep against `simple-recurring-budgets/Views/AddEditBudgetViewModel.swift`). Currency-display preference is a pure view-side concern and never reaches the VM.

## 2. Currency picker — `CurrencyPickerView`

- [x] 2.1 Create `simple-recurring-budgets/Views/CurrencyPickerView.swift` defining `struct CurrencyPickerView: View` that takes a `@Binding var selection: String` and a `@Environment(\.dismiss) var dismiss`.
- [x] 2.2 Implement a private helper that returns the catalog as `[String]` (the ISO codes), sourced from `Locale.commonISOCurrencyCodes`, sorted ascending, de-duplicated (Foundation already returns unique codes; the explicit `Set` step is defensive for future-proofing). Add a second helper `displayName(for code: String) -> String?` that returns `Locale.current.localizedString(forCurrencyCode: code)` or `nil` when the lookup fails.
- [x] 2.3 Implement the body with a `NavigationStack` containing a `List` over the filtered catalog. Each row is a `Button` showing the code on the leading edge and the localized name on the trailing edge (or just the code when no localized name). Tap → set `selection = code`; `dismiss()`. Mark the currently-selected row with `Image(systemName: "checkmark")` and `.accessibilityValue(...)` per spec key `currencyPicker.row.selected.accessibilityValue`.
- [x] 2.4 Add a `.searchable(text: $query, prompt: ...)` filter that matches by `code.localizedCaseInsensitiveContains(query)` OR `displayName.localizedCaseInsensitiveContains(query)`.
- [x] 2.5 Add a leading toolbar `Cancel` button reusing the localized key `addEditBudget.action.cancel` (per design.md → Decision 10) that calls `dismiss()` without changing `selection`.
- [x] 2.6 Set `.navigationTitle(...)` to the localized string for `currencyPicker.navigationTitle` with `.navigationBarTitleDisplayMode(.inline)`.

## 3. Sheet view — `AddEditBudgetView`

- [x] 3.1 Create `simple-recurring-budgets/Views/AddEditBudgetView.swift` defining `struct AddEditBudgetView: View` that takes `@State var viewModel: AddEditBudgetViewModel` (initialised from a value passed into `init`), reads `@Environment(\.modelContext) var context`, `@Environment(AppSettings.self) var settings`, and `@Environment(\.dismiss) var dismiss`. Add `@State private var showCurrencyPicker = false`. The `settings` env value is needed for the Allocation accessibility label (Decision 15) — do NOT pass it to `viewModel.save(...)` (it is not a parameter of `save`).
- [x] 3.2 Implement `body` with the layout from `MockupB_CardLayout.swift` adapted for production:
    - `NavigationStack { ScrollView { VStack(spacing: 16) { nameCard; allocationCard; periodCard; carryOverCard }.padding(.horizontal).padding(.top, 8).padding(.bottom, 32) } ... }`
    - `.navigationTitle(...)` localized via `addEditBudget.title.add` or `addEditBudget.title.edit` (branch on `viewModel.mode`).
    - `.navigationBarTitleDisplayMode(.inline)`.
    - Toolbar: leading `Cancel` (key `addEditBudget.action.cancel`) → `dismiss()`; trailing `Save` (key `addEditBudget.action.save`) → `viewModel.save(context: context); dismiss()`. Save button is `.disabled(!viewModel.canSave)` and `.fontWeight(.semibold)`.
    - `.appBackground()` modifier per `docs/tech-design-doc.md` §5.5.
    - `.sheet(isPresented: $showCurrencyPicker) { CurrencyPickerView(selection: $viewModel.currencyCode) }`.
- [x] 3.3 Implement `private var nameCard` as a `GroupBox` with a localized `sectionLabel` `"Name"` (key `addEditBudget.section.name`) and a `TextField` bound to `$viewModel.name`. Placeholder `"Budget"` (key `addEditBudget.field.name.placeholder`); `.font(.body)`; `.accessibilityLabel("Budget name")` (key `addEditBudget.field.name.accessibilityLabel`). Apply `.backgroundStyle(Color("CellBackground"))`.
- [x] 3.4 Implement `private var allocationCard` as a `GroupBox` titled `"Allocation"` (key `addEditBudget.section.allocation`). Inside, an `HStack` containing:
    - A `TextField("0", value: $viewModel.allocation, format: .number.precision(.fractionLength(0...2)))` (placeholder key `addEditBudget.field.allocation.placeholder`) with `.keyboardType(.decimalPad)`, `.font(.title2.weight(.semibold).monospacedDigit())`, `.frame(maxWidth: .infinity, alignment: .leading)`, and `.accessibilityLabel(...)` composed from key `addEditBudget.field.allocation.accessibilityLabel`. The accessibility label SHALL produce its formatted-amount component via `viewModel.allocation.formatted(currencyCode: viewModel.currencyCode, display: settings.currencyDisplay)` per design.md → Decision 15 — i.e. honour the user's app-wide currency-display preference (`AppSettings.currencyDisplay`). Do NOT use the older two-arg `formatted(currencyCode:locale:)` overload; use the new three-arg `formatted(currencyCode:display:locale:)` overload added by commit `a6b2e46` (`docs/tech-design-doc.md` §4.5 KV table includes `"currencyDisplay"`).
    - A currency-pill `Button` showing `Text(viewModel.currencyCode)` plus the `chevron.up.chevron.down` SF Symbol (per MockupB), `.padding`/`.background(Capsule().fill(Color.secondary.opacity(0.12)))` styling, `.foregroundStyle(.secondary)`, `.buttonStyle(.plain)`. Tap action: `showCurrencyPicker = true`. Accessibility label key `addEditBudget.field.currency.accessibilityLabel`; hint key `addEditBudget.field.currency.accessibilityHint`. The pill displays only the raw ISO code and is intentionally NOT affected by `settings.currencyDisplay`.
- [x] 3.5 Implement `private var periodCard` as a `GroupBox` titled `"Period"` (key `addEditBudget.section.period`) containing a `LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 8)` over `BudgetPeriod.allCases`, with each chip a `Button` styled per MockupB (`RoundedRectangle(cornerRadius: 8, style: .continuous)`, accent fill when selected, `.fontWeight(period == p ? .semibold : .regular)`, white foreground when selected). Tap action: `withAnimation(.easeInOut(duration: 0.15)) { viewModel.period = p }`. Each chip carries `.accessibilityLabel(...)` from key `addEditBudget.chip.period.accessibilityLabel` parameterised by `p.listLabel`, and `.accessibilityAddTraits(viewModel.period == p ? .isSelected : [])`.
- [x] 3.6 Implement `private var carryOverCard` as a `GroupBox` titled `"Carry-Over"` (key `addEditBudget.section.carryOver`). Inside, a `VStack(alignment: .leading, spacing: 0)` with:
    - `Toggle("Carry-Over", isOn: $viewModel.isCarryOverEnabled).tint(.accentColor)` and `.accessibilityHint(...)` from key `addEditBudget.toggle.carryOver.accessibilityHint`. The toggle title is the localized `addEditBudget.section.carryOver` (yes, the same key as the card label — both render "Carry-Over"; if the catalog tooling collapses these correctly, leave one entry; if it complains about the dual context, split into a second key `addEditBudget.toggle.carryOver.title` with the same default value but a distinct comment).
    - A caption `Text(...)` from key `addEditBudget.note.carryOver` (`"Accumulates unspent or overspent amounts over time."`) with `.font(.caption)`, `.foregroundStyle(.secondary)`, `.padding(.top, 8)`. Caption is visible regardless of toggle state per design.md → Decision 7.
- [x] 3.7 Implement a private helper `private func sectionLabel(_ text: String) -> some View { Text(text).font(.subheadline).foregroundStyle(.secondary) }` for use as the `GroupBox` label in cards. Consider whether `text` should be a `LocalizedStringKey` or a localized `String`; since each call site already passes a localized `String(localized:)`, accepting `String` is fine.
- [x] 3.8 Verify the screen renders correctly under `#Preview` blocks for: Add light/dark, Edit light/dark (against an in-memory `ModelContainer` from `PreviewContainer`), `xxxLarge` Dynamic Type, a representative monthly-budget seed, and an empty-fields Save-disabled state. Mirror the preview matrix from `MockupB_CardLayout`. Each preview SHALL inject `.environment(AppSettings())` so the Allocation accessibility label can resolve `settings.currencyDisplay`. Previews that render `AddEditBudgetView` directly (i.e. NOT through `RootView`) do NOT need to inject `Router` or `SyncStatus`. (Note: `DraftBudget` and the other mockup helpers will be deleted in §6, so build the preview seeds directly from `Budget` fixtures or inline literals — do NOT reach into `_Mockups/` for sample data.)

## 4. Wire `RootView` and remove placeholders

- [x] 4.1 Edit `simple-recurring-budgets/Views/RootView.swift` to add `@Environment(AppSettings.self) private var settings` (note: this requires that `RootView` is rendered with `.environment(AppSettings(...))` already in place, which `simple_recurring_budgetsApp` does).
- [x] 4.2 Replace the `case .addBudget: Text("Add Budget")` arm with `case .addBudget: AddEditBudgetView(viewModel: AddEditBudgetViewModel(settings: settings))`.
- [x] 4.3 Replace the `case .editBudget: Text("Edit Budget")` arm with `case .editBudget(let budget): AddEditBudgetView(viewModel: AddEditBudgetViewModel(editing: budget))`. (The current sheet-binding switch in `RootView.swift` does not destructure the associated value; this task requires adjusting the `switch` to bind `.editBudget(let budget)`.)
- [x] 4.4 Leave the `.addExpense` and `.viewExpense` arms unchanged (their placeholder behaviour is still in scope per the modified `app-navigation` spec). The `.settings` arm is already wired to the real `SettingsView()` (per change `2026-04-28-settings-screen`); do NOT touch it.
- [x] 4.5 Verify the existing `RootView` preview still compiles. The current preview already injects `.modelContainer(PreviewContainer.make())`, `.environment(Router())`, `.environment(AppSettings())`, AND `.environment(SyncStatus(containerBacking: .cloudKit, accountStatus: .available))` — the `SyncStatus` env value was added by commit `a6b2e46` and is required for `SettingsView` to compile inside `RootView`. Do NOT remove the `SyncStatus` injection. If you add additional `RootView`-rooted previews for opening the Add Budget / Edit Budget sheet, mirror the same env-injection list (Router, AppSettings, SyncStatus, modelContainer); seed the Edit-mode preview from a `Budget` fixture (e.g. `DebugData` if available, or an inline `Budget` insert).

## 5. Localization — `Resources/Localizable.xcstrings`

- [x] 5.1 Confirm that all new user-facing strings introduced in §1–§4 are written via `String(localized: "key", defaultValue: "...", comment: "translator context")` (or equivalent `Text(LocalizedStringKey)` with the same key + comment elsewhere). Run a `rg "Text\(\""` and `rg "String\(\""` over `simple-recurring-budgets/Views/AddEditBudgetView.swift`, `simple-recurring-budgets/Views/AddEditBudgetViewModel.swift`, and `simple-recurring-budgets/Views/CurrencyPickerView.swift` to verify no hard-coded English strings remain.
- [x] 5.2 Build the app (`make test` is fine; even a compile is enough) so the catalog tool extracts the new keys automatically. Verify `Localizable.xcstrings` now contains entries for:
    - `addEditBudget.title.add`, `addEditBudget.title.edit`
    - `addEditBudget.action.cancel`, `addEditBudget.action.save`
    - `addEditBudget.section.name`, `addEditBudget.field.name.placeholder`, `addEditBudget.field.name.accessibilityLabel`
    - `addEditBudget.section.allocation`, `addEditBudget.field.allocation.placeholder`, `addEditBudget.field.allocation.accessibilityLabel`
    - `addEditBudget.field.currency.accessibilityLabel`, `addEditBudget.field.currency.accessibilityHint`
    - `addEditBudget.section.period`, `addEditBudget.chip.period.accessibilityLabel`
    - `addEditBudget.section.carryOver`, `addEditBudget.toggle.carryOver.accessibilityHint`, `addEditBudget.note.carryOver`
    - `currencyPicker.navigationTitle`, `currencyPicker.search.prompt`, `currencyPicker.row.selected.accessibilityValue`
    Each entry SHALL have a non-empty `comment`.
- [x] 5.3 Hand-prune any catalog entries that are now stale because the mockups were deleted (e.g., the auto-extracted `"%@ period"` from `MockupB_CardLayout`, `"Allocation amount, %@ %@"` mockup version, `"Add Budget"`/`"Edit Budget"`/`"Save"`/`"Cancel"` literals from the mockup files). Acceptance: the catalog must not contain unreferenced English-only entries that originated solely from `_Mockups/`. (Re-extracted entries that happen to share the same default text but live under namespaced keys, e.g. `addEditBudget.title.add`, are fine.)

## 6. Delete the `_Mockups/` tree

- [x] 6.1 Delete `simple-recurring-budgets/Views/_Mockups/AddEditBudget/MockupA_GroupedForm.swift`.
- [x] 6.2 Delete `simple-recurring-budgets/Views/_Mockups/AddEditBudget/MockupB_CardLayout.swift`.
- [x] 6.3 Delete `simple-recurring-budgets/Views/_Mockups/AddEditBudget/MockupC_FlowLayout.swift`.
- [x] 6.4 Delete `simple-recurring-budgets/Views/_Mockups/AddEditBudget/MockupShared.swift`.
- [x] 6.5 Remove the now-empty `simple-recurring-budgets/Views/_Mockups/AddEditBudget/` and `simple-recurring-budgets/Views/_Mockups/` directories from the working tree (and ensure git records the directory removal).
- [x] 6.6 Run `rg "DraftBudget|CurrencyPickerStub|MockupA_GroupedForm|MockupB_CardLayout|MockupC_FlowLayout|mockPerLabel"` against the entire repo. There SHALL be zero matches in production source files. (Matches inside `openspec/changes/**`, `docs/**`, and tests are acceptable only if the surrounding context is design-history narrative; production-file matches are a fail.)

## 7. Tests (Swift Testing)

- [x] 7.1 Create `simple-recurring-budgetsTests/Views/AddEditBudgetViewModelTests.swift` (create the `Views` test directory if it does not exist) with `@MainActor` `@Test` cases against an in-memory `ModelContainer` from `simple-recurring-budgetsTests/Helpers/TestModelContainer.swift`:
    - **7.1.a** Add-mode defaults: `init(settings:)` produces the documented field values for both `defaultCarryOverEnabled = true` and `false`. Verify `name == "Budget"`, `allocation == 10`, `period == .daily`, currency code equals `Locale.current.currency?.identifier ?? "USD"`, and `isCarryOverEnabled` matches `settings.defaultCarryOverEnabled`.
    - **7.1.b** Edit-mode seeding: build a `Budget` with non-default values; `init(editing:)` produces a VM whose fields match each `Budget` field. Include a test case where the stored `period` raw value is unrecognised; assert the VM falls back to `.daily`.
    - **7.1.c** `canSave`: enumerate name + allocation combinations covering the spec scenarios — empty-name disabled; whitespace-only-name disabled; `allocation == 0` disabled; `allocation < 0` disabled (test by assigning a negative `Decimal` directly); positive allocation + non-empty name enabled.
    - **7.1.d** Add-mode `save(context:)` inserts exactly one `Budget` with the drafted values; `sortOrder` matches `Budget.nextSortOrder(for: context)` (test against an empty store and against a store with two existing budgets).
    - **7.1.e** Edit-mode `save(context:)` no-op: open the VM with no field changes and call `save`; assert `lastModified` is unchanged from the pre-save value.
    - **7.1.f** Edit-mode `save(context:)` single-field change: change only the name; assert the `Budget`'s `name` is updated, `lastModified` is updated to a value `>=` a pre-call `Date()`, and other fields (`allocation`, `currencyCode`, `period`, `isCarryOverEnabled`, `carryOverAmount`, `carryOverLastProcessedDate`, `carryOverLastResetDate`, `resetCadence`, `sortOrder`, `createdAt`) are untouched.
    - **7.1.g** Edit-mode `save(context:)` multi-field change: change name and allocation; assert both fields are written and `lastModified` is updated exactly once. (Verified by reading the `Budget`'s `lastModified` once after the call; comparing-equal across two reads is sufficient because the call is synchronous.)
    - **7.1.h** Cancel semantics (no `save(...)` invocation): construct a VM, mutate its fields, do NOT call `save`; assert the `Budget` rows in the store retain their original values.
    - **7.1.i** Save signature: assert at the type-system level (via a non-throwing call site that compiles) that `viewModel.save(context: context)` is the canonical call shape and that no overload accepting `AppSettings` exists. (A simple compile-time check via a generic call-site reference is sufficient; this protects against accidental re-introduction of an unused parameter.)
- [x] 7.2 Create `simple-recurring-budgetsTests/Views/CurrencyPickerTests.swift` with `@Test` cases against the picker's pure helpers:
    - **7.2.a** `catalogCodes()` returns exactly `Locale.commonISOCurrencyCodes` (sorted ascending, de-duplicated). Spot-check that `"USD"`, `"EUR"`, and `"GBP"` are all present.
    - **7.2.b** `displayName(for:)` returns `Locale.current.localizedString(forCurrencyCode:)` for known codes; returns `nil` (or falls back) for an empty / nonsense code.
    - **7.2.c** `filter(_:by:)` (whatever name you choose for the search filter helper) matches a query against either the code OR the localized name, case-insensitively, using `localizedCaseInsensitiveContains` for the name. Verify `"eu"` includes `EUR`; `"usd"` includes `USD`; `"yen"` includes `JPY` (when the localized name in en-US is "Japanese Yen").
    - **7.2.d** Empty query returns the whole catalog unchanged.
- [x] 7.3 If the Allocation accessibility-label composition is factored into a small pure helper (e.g. a `private static func allocationAccessibilityLabel(allocation:currencyCode:display:)` on `AddEditBudgetView` or a free function in the same file), add a `simple-recurring-budgetsTests/Views/AddEditBudgetAccessibilityTests.swift` covering:
    - **7.3.a** With `display: .symbol`, the produced string contains the symbol-form formatted amount (e.g. `"$25.00"` for `currencyCode: "USD"` and `Locale(identifier: "en_US")`).
    - **7.3.b** With `display: .code`, the produced string contains the ISO-code form (e.g. `"USD 25.00"`).
    - **7.3.c** With `display: .codeAndSymbol`, the produced string contains the code-prefixed form (e.g. `"USD $25.00"`).
    - **7.3.d** Each test passes a deterministic `Locale` so results are stable across CI environments.
    If the helper is not factored out (i.e. the label is computed inline in `body`), skip this task — the unit-test coverage for the formatter itself already lives in `simple-recurring-budgetsTests/Formatting/FormattersTests.swift`. Document in the implementation PR which path was chosen.
- [x] 7.4 Confirm none of the existing tests in `simple-recurring-budgetsTests/Domain/`, `simple-recurring-budgetsTests/Models/`, `simple-recurring-budgetsTests/Settings/`, `simple-recurring-budgetsTests/Formatting/`, or `simple-recurring-budgetsTests/Views/BudgetsViewTests.swift` need to change as a result of this work. Run `rg "DraftBudget|CurrencyPickerStub|mockPerLabel"` over `simple-recurring-budgetsTests/` to confirm no test references mockup symbols.

## 8. Documentation updates

- [x] 8.1 Edit `docs/product-features-planning.md` F-2.03 (Add/Edit Budget screen): change `**Status:** Open` → `**Status:** Implemented (excluding paused Reset Cadences)`. Leave the existing PAUSED note unchanged. Add a one-line implementation note pointing at this change name and clarifying that the implementation uses Foundation's system currency catalog. Do NOT remove the paused Reset Cadence acceptance bullets — they remain in the doc as future reference per the existing PAUSED protocol.
- [x] 8.2 Edit `docs/product-features-planning.md` F-3.04 (Internationalization of currency): update the third acceptance criterion to read: `- Currency catalog (codes, symbols, localized names) is sourced from outside our application source — either from Foundation's system catalog (Locale.commonISOCurrencyCodes plus Locale.localizedString(forCurrencyCode:)) or from a project-owned data file (e.g. YAML) when the app needs to diverge from the system catalog. The Add/Edit Budget screen ships the system-catalog path.` Mark the picker-on-Add/Edit-Budget acceptance criterion and the localized-name acceptance criterion as satisfied for this screen (e.g., add a parenthetical "(implemented by `add-edit-budget-screen` change)" or a status line per the doc's existing convention; pick whichever style is most consistent with neighbouring features in the file).
- [x] 8.3 Confirm `docs/tech-design-doc.md` needs no edit. Per design.md → Decision 14, none of §2.1, §3, §4, §5 materially change. **No version-history bump for this change.** If implementation surfaced a genuinely new architectural rule (it should not), append a note here describing what changed and why.
- [x] 8.4 Confirm `docs/main-prd.md` needs no edit. Grep for "Add/Edit Budget", "currency", "carry-over", "Reset Cadence" — the global constraints (§6.5, §6.7) are untouched.
- [x] 8.5 Confirm `docs/ux-design-brief.md` needs no edit. The brief's bullet on Add/Edit Budget already describes this screen.

## 9. Build, test, and smoke-check

- [x] 9.1 Run `make test` (or `bash scripts/test.sh`) per `AGENTS.md` and `.cursor/rules/ios-build-test.mdc`. All existing tests SHALL pass; the new `Views/AddEditBudgetViewModelTests.swift` and `Views/CurrencyPickerTests.swift` SHALL pass.
- [x] 9.2 Smoke test on a clean iPhone simulator (matrix from design.md → Migration Plan §4):
    - **9.2.a** Empty store → toolbar `+` → Add sheet opens with documented defaults (allocation 10, locale currency, Daily, carry-over from settings); tap Save → first `Budget` appears in the list with the expected fields.
    - **9.2.b** Add sheet → clear name (or whitespace-only) → Save disabled. Set allocation to 0 → Save still disabled. Set positive allocation + non-empty name → Save enabled.
    - **9.2.c** Add sheet → tap currency pill → picker opens; type `"eu"` in the search → list filters to currencies whose code or localized name matches; tap a row (e.g., EUR) → picker dismisses, parent sheet's currency pill shows the new code. Open picker again, tap Cancel → selection unchanged.
    - **9.2.d** Add sheet → tap each period chip in turn → only one is selected at a time; with VoiceOver enabled, focus the selected chip → it announces "<period> period, selected".
    - **9.2.e** Add sheet → toggle Carry-Over off and on → the caption stays visible in both states; Save inserts a `Budget` with the toggle's last value.
    - **9.2.f** Until F-2.02 ships, the Edit path is reached by a temporary `RootView` preview entry or by a small ad-hoc tap target wired up only for this verification. Verify Edit mode pre-fills, that no-op Save leaves `lastModified` unchanged, and that single-field Save bumps `lastModified`.
    - **9.2.g** Cancel from any state → no `Budget` is inserted or modified.
    - **9.2.h** Dynamic Type at `xxxLarge` → cards reflow without clipping; chips remain readable.
    - **9.2.i** Dark Mode → no surface reads as a hard-coded white card; backgrounds use `appBackground()` / `Color("CellBackground")`.
    - **9.2.j** With VoiceOver enabled, open the Add sheet and focus the Allocation field; note the announced amount string. Then dismiss the sheet, open Settings → Display → Currency Display, change the preference (e.g. `.symbol` → `.code`), close Settings, re-open Add Budget, focus Allocation, and confirm the announced string now reflects the new preference (e.g. `"USD 25.00"` instead of `"$25.00"`). This validates the Decision 15 plumbing end-to-end.
- [x] 9.3 Run `rg "import.*MockupShared|DraftBudget|CurrencyPickerStub|mockPerLabel|MockupA_|MockupB_|MockupC_"` over the whole repo as a final residual check; expect zero matches in production source files.

## 10. Archive this change

- [x] 10.1 After all sections 1–9 are complete and `make test` passes, run `openspec archive add-edit-budget-screen` (or follow the `openspec-archive-change` skill).
- [x] 10.2 Because this change introduces a new capability (`add-edit-budget-screen`) AND modifies an existing one (`app-navigation`), expect the archive to: (a) create `openspec/specs/add-edit-budget-screen/spec.md` from the delta, and (b) update `openspec/specs/app-navigation/spec.md`'s "Placeholder destinations are exempt..." requirement to the new wording.
- [x] 10.3 After archive, confirm: (a) `openspec/specs/add-edit-budget-screen/spec.md` exists with the requirements introduced here; (b) `openspec/specs/app-navigation/spec.md` reflects the modified placeholder requirement; (c) `openspec/changes/` no longer contains `add-edit-budget-screen/`; (d) `openspec/changes/archive/` contains a date-prefixed `add-edit-budget-screen/` directory with the proposal, design, tasks, and original delta specs preserved.
