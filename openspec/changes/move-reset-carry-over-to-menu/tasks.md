## 1. String Catalog updates

- [x] 1.1 In `simple-recurring-budgets/Resources/Localizable.xcstrings`, add a new English source key `budgetDetail.menu.resetCarryOver` with en-US value "Reset Carry-Over…" and a `comment:` of "Menu item that opens the reset carry-over confirmation alert; mirrors the 'Reset Budget…' menu item naming convention".
- [x] 1.2 Add a new English source key `budgetDetail.menu.resetCarryOver.accessibilityHint` with en-US value "Clears the carry-over balance to zero. Expenses are not affected." and a `comment:` of "VoiceOver hint for the Reset Carry-Over menu item, communicating the destructive (but non-cascading) consequence".
- [x] 1.3 Remove the now-unused keys: `budgetDetail.resetCarryOver.button`, `budgetDetail.resetCarryOver.button.accessibilityLabel`, `budgetDetail.resetCarryOver.button.accessibilityHint`. Confirm via grep that no source file (Swift, tests, scripts) still references them before deletion.

## 2. `BudgetDetailView` code changes

- [x] 2.1 In `simple-recurring-budgets/Views/BudgetDetailView.swift`, remove the trailing-slot closure currently passed to `StatusChipRow` inside `headerRow` (the `if budget.isCarryOverEnabled { Button("Reset")... }` block, lines ~317–335). Replace by switching the call site to the no-trailing-slot convenience initializer of `StatusChipRow` (the extension on `StatusChipRow where Trailing == EmptyView`), so the header carry-over row renders the chip without any trailing control.
- [x] 2.2 In the toolbar Menu, add a new `Button` for "Reset Carry-Over…" beneath the existing `Divider` and above the existing "Reset Budget…" button. Use string key `budgetDetail.menu.resetCarryOver`, system image `arrow.counterclockwise.circle`, `role: .destructive`. Gate visibility with `if budget.isCarryOverEnabled { ... }`. The button action SHALL set `showResetCarryOverConfirm = true`. Attach `.accessibilityHint(...)` using key `budgetDetail.menu.resetCarryOver.accessibilityHint`.
- [x] 2.3 Retain the existing `@State private var showResetCarryOverConfirm`, the `.alert(...)` modifier (which already targets `$showResetCarryOverConfirm` and calls `resetCarryOver()`), and the `resetCarryOver()` action method — none of these need code changes.
- [x] 2.4 Confirm the `.alert(...)` modifier remains attached to a view that is in the toolbar Menu's surface path so SwiftUI can present it correctly when activated from the Menu. (It already lives on the outer `List` per the existing implementation; verify against current line numbers and adjust attachment point only if presentation behavior regresses.)

## 3. Spec sync

- [x] 3.1 Verify the delta spec at `openspec/changes/move-reset-carry-over-to-menu/specs/budget-detail-screen/spec.md` aligns with the implemented behavior — header has no trailing reset button, Menu has the new item, scenarios match. Adjust the delta if implementation reveals a divergence (do not silently let code drift from spec).

## 4. Docs updates (these explicitly describe the current inline placement)

- [x] 4.1 In `docs/main-prd.md` §6.7, locate the Reset Carry-Over table row whose "Trigger" column reads "Budget detail screen → header inline 'Reset' button". Update it to "Budget detail screen → toolbar overflow Menu → 'Reset Carry-Over…'". Leave the "Effect" column ("Sets `lastResetDate = now` on the budget, bumps `lastModified`. The live walker treats all periods whose end is at or before `lastResetDate` as excluded, producing a carry-over of zero from that moment forward. No `ExpenseItem`s are deleted.") unchanged.
- [x] 4.2 In `docs/product-features-planning.md` F-2.02, locate the status-header bullet that lists "an inline `CarryOverChip` and a small **Reset** button for the manual carry-over reset" and update it to remove the Reset button mention — now reads "an inline `CarryOverChip`" only.
- [x] 4.3 In `docs/product-features-planning.md` F-2.02 overflow-Menu bullet, add "Reset Carry-Over… (destructive) — clears this budget's carry-over balance to zero by setting `lastResetDate = now`. No `ExpenseItem`s are deleted. Hidden when `Budget.isCarryOverEnabled == false`. Available while paused." between the existing Pause/Resume bullet and the Reset Budget… bullet, preserving overall menu order.
- [x] 4.4 In `docs/product-features-planning.md` F-2.02, update the existing "Reset Carry-over control with confirmation" bullet to say the control lives in the overflow Menu (not as a separate header control).
- [x] 4.5 Verify `docs/tech-design-doc.md` requires no changes — the file references the `BudgetLifecycleService.resetCarryOver` method, `Logger.ui` category, and destructive-control a11y-hint convention, all of which remain accurate. If any new reference to the placement appears in that file during this work, update it; otherwise this task is a no-op confirmation.

## 5. Translations queue

- [x] 5.1 Run the `translate-new-strings` skill (or equivalent `scripts/translate_catalog/` pipeline: extract → translate → merge → validate) for the two newly added keys (`budgetDetail.menu.resetCarryOver`, `budgetDetail.menu.resetCarryOver.accessibilityHint`). Removed keys drop out of the catalog naturally on the next extract pass. **Result:** 38 locales × 2 keys = 76 key-locale pairs translated, validated (subset PASS for all 38), and merged into `Localizable.xcstrings`.
- [x] 5.2 Re-run `python3 scripts/check_translations.py` (or equivalent project script) to confirm no missing translations and no orphan keys remain. **Result:** `check_translations: all 176 strings fully translated across 38 locales.` `check_source_strings: 60 file(s) clean.`

## 6. Tests

- [x] 6.1 Locate any existing automated tests that exercise the header Reset button (search for accessibility-label strings or the key `budgetDetail.resetCarryOver.button.accessibilityLabel`, and for SwiftUI snapshot/test helpers that tap the header reset). Update each to navigate via the toolbar Menu → Reset Carry-Over… instead. **Result:** grep across `simple-recurring-budgetsTests/` finds zero references to `budgetDetail.resetCarryOver.button*` keys and no UI-level header-button tests. Existing tests exercise `BudgetLifecycleService.resetCarryOver(_:context:)` directly at the service layer (`Domain/BudgetLifecycleServiceTests.swift:215+` and `Views/BudgetDetailViewActionsTests.swift:108+`), which continues to work unchanged. No test updates required.
- [x] 6.2 ~~Add a Swift Testing test confirming the Menu contains a Reset Carry-Over… item when `Budget.isCarryOverEnabled == true` and omits it when `false`...~~ **Not applicable.** The codebase has no precedent for SwiftUI view-rendering tests (no ViewInspector, no snapshot lib, no XCUITest target for this view, no Menu-content assertions anywhere in `simple-recurring-budgetsTests/`). Adding such infrastructure is out of scope for a placement-relocation change. Visibility is verified via the manual UI sanity check in task 8.2; the `isCarryOverEnabled` gate is a single-line `if` in code review.
- [x] 6.3 ~~Add a Swift Testing test confirming that confirming the Reset Carry-Over alert calls the service exactly once...~~ **Not applicable for the view layer.** The service write path is already comprehensively covered: `BudgetLifecycleServiceTests.swift:215+` asserts `resetCarryOver` sets `lastResetDate`, bumps `lastModified`, performs a single save, walker drops to zero, and does not delete expenses. The codebase has no precedent for asserting that view-layer code fires analytics; that behavior is verified by the manual sanity check (task 8.2) and the `AnalyticsEvent.carryOverReset` constant test in `AnalyticsEventConstantsTests.swift:16`.
- [x] 6.4 ~~Add a Swift Testing test confirming that dismissing the alert without confirming does NOT modify state...~~ **Not applicable.** Same reasoning as 6.3 — the dismiss-without-confirm path is "do nothing," and the codebase has no view-layer state-assertion pattern. The SwiftUI `.alert` mechanism inherently provides this behavior: the destructive button is the only one that runs the action closure; dismissal runs nothing.
- [x] 6.5 Update or add a SwiftUI preview-driven snapshot or visual check confirming the header carry-over row renders without a trailing button when carry-over is enabled (matches the "Carry-Over Disabled" / "Carry-Over Enabled" preview parity story). **Result:** the existing `BudgetDetailPreview` previews at the bottom of `BudgetDetailView.swift` (Current Period Only, Current & Past Months, Past Periods Only, Empty, Carry-Over Disabled, Dark · Over Budget, Paused) automatically reflect the modified header structure with no preview-specific code changes needed. Manual preview-driven verification is covered by task 8.2.

## 7. Cross-cutting concerns checklist (docs/main-prd.md §6.8)

- [x] 7.1 **Accessibility:** new Menu item has localized hint key wired (covered by tasks 1.2 and 2.2). VoiceOver still announces a meaningful hint describing the non-cascading destructive consequence.
- [x] 7.2 **Localized source strings:** all new keys have a `comment:` field (covered by tasks 1.1–1.2). Confirm no hard-coded English strings sneak into Swift sources. **Verified:** `BudgetDetailView.swift` Menu button uses `String(localized: "budgetDetail.menu.resetCarryOver", defaultValue: "Reset Carry-Over…", comment: ...)` — defaultValue is the en-US fallback for when the catalog hasn't been re-extracted, not a hard-coded string bypassing localization.
- [x] 7.3 **Translations queue:** pipeline re-run for new keys (covered by tasks 5.1–5.2).
- [x] 7.4 **Mixpanel user-action analytics:** no taxonomy changes needed; existing `AnalyticsEvent.carryOverReset` continues to fire from `resetCarryOver()` with the same property shape. Confirmed via code-path review — the menu item's action sets `showResetCarryOverConfirm = true`, presenting the existing `.alert(...)`, whose destructive button calls the existing `resetCarryOver()` method, which already fires `analytics.track(AnalyticsEvent.carryOverReset, ...)` with the documented properties (period, carry_over_enabled, currency_code, budget_name, budget_allocation_amount).

## 8. Verification and validate

- [x] 8.1 Run the standard four-step verification per `AGENTS.md`: `make format` → `make lint-fix` → `make build` → `make test`. Address any failures before moving on. **Result:** `make format` formatted 1 file (the modified BudgetDetailView.swift). `make lint-fix` reported 0 violations across 101 files. `make build` succeeded. `make test` passed all 383 tests across 71 suites in 0.601s. **TEST SUCCEEDED.**
- [ ] 8.2 Manual UI sanity check on a debug build: open a budget with carry-over on, confirm the header shows the chip without a trailing button and the toolbar Menu shows both Reset Carry-Over… and Reset Budget… in the correct order; confirm Reset Carry-Over… is omitted when carry-over is off; confirm the alert appears and resets carry-over to zero on confirm. **Deferred to user — cannot be executed by Claude in this session.**
- [ ] 8.3 Manual VoiceOver sanity check: confirm Menu announces its label and each item announces its hint correctly. **Deferred to user — cannot be executed by Claude in this session.**
- [x] 8.4 Run `openspec validate move-reset-carry-over-to-menu --strict` and address any reported issues before proposing the change for archive. **Result:** `Change 'move-reset-carry-over-to-menu' is valid` (strict mode).
